================================================================================
ROOTUI.TSCN SCENE RESTRUCTURING SPECIFICATION
================================================================================

This specification tells you exactly what to do in Godot's Scene tree to 
restructure RootUI.tscn. Follow the steps in order.

================================================================================
STEP 1: COMPRESSPANELCONTAINER (Left Stack)
================================================================================

Location: TopBandHBox → LeftStackVBox → CompressPanelContainer → CompressMargin → CompressVBox

CURRENT STRUCTURE (keep these):
├── MonadCompressButton
│   └── ButtonLabelInstance
│       ├── TopLabel
│       └── BottomLabel
├── MonadCooldownBar
└── MonadCountersVBox
    ├── RowSolidMonadInstance
    ├── RowLiquidMonadInstance
    └── RowGasMonadInstance

TO DELETE (select in scene tree, right-click → Delete):
- ParticleCompressButton (line 775)
- ParticleCooldownBar (line 791)  
- RowParticleInstance (line 797)
- MoteCompressButton (line 805)
- MoteCooldownBar (line 821)
- RowMoteInstance (line 827)

AFTER DELETION - ADD these from Right Stack (see Step 2 for source):
Add these nodes AS SIBLINGS after MonadCountersVBox (drag from right or recreate):

1. UoniteCooldownBar
   - Find in: RightStackVBox → AssemblePanelContainer → AssembleMargin → AssembleTopHBox → AssembleIotaGrainVBox
   - Move it to: LeftStackVBox → CompressPanelContainer → CompressMargin → CompressVBox (after MonadCountersVBox)
   - Settings: custom_minimum_size = Vector2(0, 6)

2. CreateUoniteButton
   - Find in: RightStackVBox → AssemblePanelContainer → AssembleMargin → AssembleTopHBox → AssembleIotaGrainVBox
   - Move it to: LeftStackVBox → CompressPanelContainer → CompressMargin → CompressVBox (after UoniteCooldownBar)
   - Settings: custom_minimum_size = Vector2(140, 55)

3. ButtonLabelInstance (child of CreateUoniteButton)
   - Move with CreateUoniteButton
   - Settings: resource_key = "uonite"

4. UoniteCreationPanel (PanelContainer)
   - Find in: RightStackVBox → AssemblePanelContainer → AssembleMargin → AssembleTopHBox → AssembleIotaGrainVBox
   - Move to: LeftStackVBox → CompressPanelContainer → CompressMargin → CompressVBox (after ButtonLabelInstance)
   - Settings: custom_minimum_size = Vector2(142, 142)

5. UoniteCreationViewport (SubViewportContainer)
   - Child of UoniteCreationPanel
   - Move with parent
   - Settings: custom_minimum_size = Vector2(142, 142), stretch = true

6. UoniteCreationSubViewport (SubViewport)
   - Child of UoniteCreationViewport
   - Move with parent
   - Settings: own_world_3d = true, transparent_bg = true, size = Vector2i(142, 142)

7. UoniteIcosahedron (MeshInstance3D, instance)
   - Child of UoniteCreationSubViewport
   - Move with parent chain
   - Keep current settings

8. UoniteCamera (Camera3D)
   - Child of UoniteCreationSubViewport
   - Move with parent chain


================================================================================
STEP 2: ASSEMBLETOPHBOX (Right Stack)
================================================================================

Location: TopBandHBox → RightStackVBox → AssemblePanelContainer → AssembleMargin → AssembleTopHBox

CURRENT STRUCTURE:
├── AssembleTetradVBox  ← KEEP THIS IDENTICAL, DO NOT TOUCH
└── AssembleIotaGrainVBox ← RENAME THIS

TO DO:

A) RENAME AssembleIotaGrainVBox → AssembleChainVBox
   - Right-click node in scene tree → Rename
   - New name: AssembleChainVBox

B) ADD nodes FROM LEFT STACK (moved in Step 1)

   The left stack CompressVBox after deletion should have these nodes that 
   need to be MOVED to AssembleChainVBox:

   1. ParticleCompressButton → Move to AssembleChainVBox
      - Source: LeftStackVBox/CompressPanelContainer/CompressMargin/CompressVBox/ParticleCompressButton
      - Destination: AssembleChainVBox (as first child)
      - Keep all children: ButtonLabelInstance → TopLabel, BottomLabel

   2. ParticleCooldownBar → Move to AssembleChainVBox
      - Source: LeftStackVBox/CompressPanelContainer/CompressMargin/CompressVBox/ParticleCooldownBar
      - Destination: AssembleChainVBox (after ParticleCompressButton)

   3. RowParticleInstance → Move to AssembleChainVBox
      - Source: LeftStackVBox/CompressPanelContainer/CompressMargin/CompressVBox/RowParticleInstance
      - Destination: AssembleChainVBox (after ParticleCooldownBar)
      - Keep child: ResourceLabel

   4. MoteCompressButton → Move to AssembleChainVBox
      - Source: LeftStackVBox/CompressPanelContainer/CompressMargin/CompressVBox/MoteCompressButton
      - Destination: AssembleChainVBox (after RowParticleInstance)
      - Keep all children: ButtonLabelInstance → TopLabel, BottomLabel

   5. MoteCooldownBar → Move to AssembleChainVBox
      - Source: LeftStackVBox/CompressPanelContainer/CompressMargin/CompressVBox/MoteCooldownBar
      - Destination: AssembleChainVBox (after MoteCompressButton)

   6. RowMoteInstance → Move to AssembleChainVBox
      - Source: LeftStackVBox/CompressPanelContainer/CompressMargin/CompressVBox/RowMoteInstance
      - Destination: AssembleChainVBox (after MoteCooldownBar)
      - Keep child: ResourceLabel


================================================================================
STEP 3: VERIFY ASSEMBLECHAINVBOX STRUCTURE
================================================================================

After all moves, AssembleChainVBox should contain (in order):

1. ParticleCompressButton
   └── ButtonLabelInstance
       ├── TopLabel
       └── BottomLabel
2. ParticleCooldownBar
3. RowParticleInstance
   └── ResourceLabel
4. IotaAssembleButton
   └── ButtonLabelInstance
5. IotaCooldownBar
6. RowIotaInstance
7. MoteCompressButton
   └── ButtonLabelInstance
       ├── TopLabel
       └── BottomLabel
8. MoteCooldownBar
9. RowMoteInstance
   └── ResourceLabel
10. GrainAssembleButton
    └── ButtonLabelInstance2
        ├── TopLabel
        └── BottomLabel
11. GrainCooldownBar
12. RowGrainInstance
    └── ResourceLabel

NOTE: CreateUoniteButton, UoniteCooldownBar, and UoniteCreationPanel should 
NO LONGER be in AssembleChainVBox - they were moved to CompressPanelContainer 
in Step 1.


================================================================================
STEP 4: VERIFY COMPRESSPANELCONTAINER STRUCTURE
================================================================================

After all moves, CompressVBox should contain (in order):

1. MonadCompressButton
   └── ButtonLabelInstance
       ├── TopLabel
       └── BottomLabel
2. MonadCooldownBar
3. MonadCountersVBox
   ├── RowSolidMonadInstance
   ├── RowLiquidMonadInstance
   └── RowGasMonadInstance
4. UoniteCooldownBar (moved from right)
5. CreateUoniteButton (moved from right)
   └── ButtonLabelInstance (moved with parent)
6. UoniteCreationPanel (moved from right)
   └── UoniteCreationViewport
       └── UoniteCreationSubViewport
           ├── UoniteIcosahedron (moved with parent chain)
           └── UoniteCamera (moved with parent chain)


================================================================================
STEP 5: UPDATE ROOT_UI.GD (ALREADY DONE)
================================================================================

Already completed - both paths updated from AssembleIotaGrainVBox to AssembleChainVBox:
- Line 745: CreateUoniteButton path
- Line 771: UoniteIcosahedron path


================================================================================
VERIFICATION CHECKLIST
================================================================================

□ CompressPanelContainer has exactly 6 children (Monads + Uonite items)
□ AssembleChainVBox has exactly 12 children (Particle, Iota, Mote, Grain)
□ AssembleTetradVBox unchanged on left side of AssembleTopHBox
□ root_ui.gd paths verified (AssembleChainVBox)
□ No orphaned nodes anywhere
□ Scene runs without errors

================================================================================
END OF SPECIFICATION
================================================================================