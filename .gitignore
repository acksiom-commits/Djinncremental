# Godot-specific ignores
.godot/
*.import
export_presets.cfg
debug.log

# dev_tests/ is TRACKED (see dev_tests/README.md) — it includes
# test_matrix_up_lint.gd, a standing guard against a recurring class of
# bug, and it has to survive a fresh clone to do that job.

# Windows-specific ignores
Thumbs.db
desktop.ini

# macOS-specific ignores
.DS_Store

# Mobile/Web export ignores
*.vsproj
*.sln